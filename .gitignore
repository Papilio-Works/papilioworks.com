# Docusaurus build output — built fresh by CI on every deploy
# Leading / anchors this to the repo root, so docs-src/docs/ is NOT excluded
/docs/

# Docusaurus local dev
docs-src/.docusaurus/
docs-src/node_modules/
